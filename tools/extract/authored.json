{
  "_comment": "Files written fresh in this repository rather than ported. The gate requires every file to appear either here or in manifest.json.",
  "files": {
    ".agents/commands/coord-claim.md": "public Codex slash command: claim a row after checking it can be closed, with a step, a write set, and a claim before any fan-out",
    ".agents/commands/coord-close.md": "public Codex slash command: close a row against its declared proof, or park/block with a durable resume contract",
    ".agents/commands/coord-handoff.md": "public Codex slash command: move responsibility for a row to the other lane with exact fences and evidence the receiver can act on",
    ".agents/commands/coord-recover.md": "public Codex slash command: reconcile a row when the board and reality disagree -- stale claims, dead sessions, blocked rows, overlapping write sets",
    ".agents/commands/coord-start.md": "public Codex slash command: orient against the board before claiming anything, under one stable session identity",
    ".agents/skills/operating-coordharness/SKILL.md": "public Codex operator skill package",
    ".agents/skills/operating-coordharness/agents/openai.yaml": "public Codex skill agent metadata",
    ".agents/skills/operating-coordharness/references/context.md": "public Codex skill context reference",
    ".agents/skills/operating-coordharness/references/jobs.md": "public Codex skill jobs reference",
    ".agents/skills/operating-coordharness/references/lifecycle.md": "public Codex skill lifecycle reference",
    ".claude-plugin/marketplace.json": "public single-plugin marketplace manifest for the coordharness Claude Code plugin",
    ".claude-plugin/plugin.json": "public Claude Code plugin manifest declaring the skill package and the five slash commands",
    ".claude/commands/coord-claim.md": "public Claude slash command, mirrored byte-for-byte from .agents/commands/coord-claim.md: claim a row after checking it can be closed",
    ".claude/commands/coord-close.md": "public Claude slash command, mirrored byte-for-byte from .agents/commands/coord-close.md: close a row against its declared proof, or park/block",
    ".claude/commands/coord-handoff.md": "public Claude slash command, mirrored byte-for-byte from .agents/commands/coord-handoff.md: move responsibility for a row to another lane",
    ".claude/commands/coord-recover.md": "public Claude slash command, mirrored byte-for-byte from .agents/commands/coord-recover.md: reconcile a row when the board and reality disagree",
    ".claude/commands/coord-start.md": "public Claude slash command, mirrored byte-for-byte from .agents/commands/coord-start.md: orient against the board before claiming anything",
    ".claude/skills/operating-coordharness/SKILL.md": "public Claude operator skill package",
    ".claude/skills/operating-coordharness/agents/openai.yaml": "public Claude skill agent metadata",
    ".claude/skills/operating-coordharness/references/context.md": "public Claude skill context reference",
    ".claude/skills/operating-coordharness/references/jobs.md": "public Claude skill jobs reference",
    ".claude/skills/operating-coordharness/references/lifecycle.md": "public Claude skill lifecycle reference",
    ".codex/config.toml": "public project-local Codex MCP configuration using clone-relative paths",
    ".codex/templates/claude-mcp.json": "generated Claude MCP client configuration template with placeholders only",
    ".codex/templates/codex-config.toml": "generated Codex MCP client configuration template with placeholders only",
    ".devcontainer/Dockerfile": "minimal devcontainer/CI image for trying the harness or running it in CI, written fresh; see docs/containers.md",
    ".devcontainer/devcontainer.json": "VS Code devcontainer configuration binding the Dockerfile, forwarding the board port, and running coord doctor on create",
    ".dockerignore": "keeps the local database, venv, build output, and git history out of the container build context; see docs/containers.md",
    ".github/CODE_OF_CONDUCT.md": "written fresh for this repository",
    ".github/CONTRIBUTING.md": "contributor guide, written fresh",
    ".github/ISSUE_TEMPLATE/bug.yml": "written fresh for this repository",
    ".github/ISSUE_TEMPLATE/config.yml": "written fresh for this repository",
    ".github/ISSUE_TEMPLATE/feature.yml": "written fresh for this repository",
    ".github/SECURITY.md": "written fresh for this repository",
    ".github/dependabot.yml": "written fresh for this repository",
    ".github/privacy-denylist.sha256": "opaque publication privacy denylist",
    ".github/pull_request_template.md": "written fresh for this repository",
    ".github/scripts/validate_docs.py": "written fresh for this repository",
    ".github/workflows/native.yml": "written fresh for this repository",
    ".github/workflows/pages.yml": "CI workflow building tools/export_static_board.py's output and uploading it as a Pages artifact; deploy is a separate manual, operator-gated job",
    ".github/workflows/python.yml": "written fresh for this repository",
    ".github/workflows/release.yml": "CI workflow publishing a tagged release to PyPI over Trusted Publishing (OIDC); inert until the operator completes the one-time PyPI/environment setup",
    ".github/workflows/security-publication.yml": "written fresh for this repository",
    ".gitignore": "written fresh for this repository",
    ".mcp.json": "public clone-relative MCP configuration for compatible clients",
    "AGENTS.md": "public agent operating contract for standalone COORD",
    "CHANGELOG.md": "public release history, written fresh",
    "CLAUDE.md": "public Claude operating contract for standalone COORD",
    "CODE_OF_CONDUCT.md": "Contributor Covenant 2.1 code of conduct, written fresh for this repository",
    "CONTRIBUTING.md": "contributor guide, written fresh for this repository",
    "LICENSE": "written fresh for this repository",
    "MANIFEST.in": "explicit complete-test-suite source-distribution manifest",
    "README.md": "front door, written fresh",
    "SECURITY.md": "root security policy, written fresh for this repository -- the single policy of record; .github/SECURITY.md is a pointer to it",
    "apps/.gitignore": "written fresh for this repository",
    "apps/Fixtures/snapshot-v1.json": "Fixture snapshot of the v1 native board contract, read by the app tests.",
    "apps/Fixtures/usage-dashboard-live-v1.json": "deterministic synthetic fresh-usage fixture for native client refresh tests",
    "apps/Fixtures/usage-dashboard-v1.json": "deterministic synthetic provider-usage fixture for native rendering and decoding tests",
    "apps/README.md": "written fresh for this repository",
    "apps/Shared/Sources/CockpitModel.swift": "native client source",
    "apps/Shared/Sources/DesignTokens.swift": "native client source",
    "apps/Shared/Sources/NativeSnapshot.swift": "native client source",
    "apps/Shared/Sources/SnapshotCache.swift": "native client source",
    "apps/Shared/Sources/SnapshotClient.swift": "native client source",
    "apps/Shared/Sources/Surfaces.swift": "native client source",
    "apps/Shared/Sources/Theme.swift": "native client source",
    "apps/Shared/Sources/UsageDashboardView.swift": "thin standalone wrapper around the shared usage dashboard content written fresh",
    "apps/Tests/ConfigPersistenceTests.swift": "standalone atomic configuration persistence regressions written fresh",
    "apps/Tests/CoordDatabasePathTests.swift": "standalone database path-precedence and containment regressions written fresh",
    "apps/Tests/EndpointTestFixtures.swift": "neutral endpoint-only XCTest fixtures written fresh",
    "apps/Tests/HarnessEndpointTests.swift": "standalone endpoint selection regressions written fresh",
    "apps/Tests/MenuBarPerformanceTests.swift": "bounded menu-bar scheduling and refresh performance regressions written fresh",
    "apps/Tests/MenubarBucketingTests.swift": "regressions holding the section rule: paused work is never filed as up-next with or without a bucket, an unknown status never overrides the server's bucket, and a group key is read as a section name only when no bucket was sent; written fresh",
    "apps/Tests/NativeDetailPolishTests.swift": "native detail hierarchy and layout contract regressions written fresh",
    "apps/Tests/NativeNavigationTests.swift": "native compact-navigation visibility regressions written fresh",
    "apps/Tests/NativeOperatorWritesTests.swift": "native XCTest regression asserting the native-operator action broker binds only to the fixed loopback endpoint with a private, restrictively-permissioned bearer token, checked by reading NativeOperatorTokenSource.swift and NativeCockpitActionBroker.swift as source text rather than a live network probe",
    "apps/Tests/NativeSnapshotTests.swift": "native client source",
    "apps/Tests/PowerControlTests.swift": "native XCTest coverage for the local power subsystem written fresh: pmset battery, charge-limit, and energy-mode parsing, its fail-closed error paths, and the derived battery/caffeine presentation state",
    "apps/Tests/ReviewHardeningTests.swift": "written fresh for this repository",
    "apps/Tests/SnapshotClientTests.swift": "native client source",
    "apps/Tests/StatusItemClickRoutingTests.swift": "status-item primary and secondary click routing regressions written fresh",
    "apps/Tests/StatusMenuActionQueueTests.swift": "status-menu deferred action serialization regressions written fresh",
    "apps/Tests/SystemTelemetryDecodeTests.swift": "standalone bounded system-telemetry decoding regressions written fresh",
    "apps/Tests/SystemTelemetryDisplayPolicyTests.swift": "menu-bar telemetry severity and compact geometry regressions written fresh",
    "apps/Tests/UsageAccountActionsTests.swift": "standalone account-action transport, redaction, and failure regressions written fresh",
    "apps/Tests/UsageDashboardTests.swift": "standalone usage decoding, retention, layout, and presentation regressions written fresh",
    "apps/Tests/WordmarkClickRoutingTests.swift": "native XCTest regression asserting the menu-bar wordmark button routes its click through PanelAction.openCockpit end to end, checked by reading ContentStackAndRows.swift and PopoverController.swift as source text",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/Contents.json": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_128x128.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_128x128@2x.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_16x16.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_16x16@2x.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_256x256.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_256x256@2x.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_32x32.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_32x32@2x.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_512x512.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/AppIcon.appiconset/icon_512x512@2x.png": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets.xcassets/Contents.json": "application icon, generated from the maintainer's 1024px master",
    "apps/brand/Assets/README.md": "how to substitute owner marks in a build",
    "apps/brand/Assets/accelerator.png": "owner mark; maintainer's own artwork, resized and stripped of authoring metadata",
    "apps/brand/Assets/claude-menu.png": "maintainer artwork; compact chat-agent provider identifier with exact provenance in docs/assets/provenance.json",
    "apps/brand/Assets/claude.png": "owner mark; maintainer's own artwork, resized and stripped of authoring metadata",
    "apps/brand/Assets/codex-menu.png": "maintainer artwork; compact code-agent provider identifier with exact provenance in docs/assets/provenance.json",
    "apps/brand/Assets/codex.png": "owner mark; maintainer's own artwork, resized and stripped of authoring metadata",
    "apps/brand/Assets/compute.png": "owner mark; maintainer's own artwork, resized and stripped of authoring metadata",
    "apps/brand/Assets/wordmark.png": "maintainer-supplied COORD wordmark with exact provenance in docs/assets/provenance.json",
    "apps/iOS/Sources/CoordCockpitIOSApp.swift": "native client source",
    "apps/iOS/Sources/IOSRootView.swift": "native client source",
    "apps/iOS/Sources/IOSWebSurface.swift": "native client source",
    "apps/install.sh": "build and install the Release mac apps to /Applications",
    "apps/macOS/Sources/CoordCockpitMacApp.swift": "native client source",
    "apps/macOS/Sources/MacCockpitView.swift": "native client source",
    "apps/macOS/Sources/MenuSummaryView.swift": "native client source",
    "apps/menubar/Sources/App/AgentMarks.swift": "owner marks drawn as vector code; no image files, nothing to license",
    "apps/menubar/Sources/App/DeferredCoalescedValue.swift": "small generic main-thread coalescing primitive written fresh for menu-bar refresh stability",
    "apps/menubar/Sources/App/HarnessEndpoint.swift": "written fresh for this repository",
    "apps/menubar/Sources/App/LocalPowerControls.swift": "local, non-networked power subsystem written fresh: pmset battery/charge-limit/energy-mode parsing that fails closed rather than inventing a percentage, the LocalPowerController driving those reads, a dedicated dockable battery status item, and the caffeine, battery-meter, and battery-details controls views",
    "apps/menubar/Sources/App/StatsStatusItemController.swift": "written fresh: a second, independently dockable menu-bar status item devoted to system-telemetry rings, kept separate from the primary quota status item so enabling Stats never paints the same metrics twice",
    "apps/menubar/Sources/App/StatusItemClickRouting.swift": "pure status-item click destination resolver written fresh",
    "apps/menubar/Sources/Cockpit/Core/NativeOperatorTokenSource.swift": "written fresh: resolves the native-operator bearer token from the environment or a private, owner-only on-disk file, and fixes the action broker's endpoint to 127.0.0.1 on HarnessEndpoint.defaultPort so a native write can only ever reach this machine's own harness process",
    "apps/menubar/Sources/Cockpit/Data/DeclinedRoutes.swift": "remembers routes the server declined so probes are not retried on a timer",
    "apps/menubar/Sources/Cockpit/UI/CockpitMapWebView.swift": "native client source; began as a port and now diverges deliberately — this window embeds the harness's own Mesh/Map/Atlas routes, the source product embeds its own console routes",
    "apps/menubar/Sources/Cockpit/UI/CockpitRootView.swift": "native client source; began as a port and now diverges deliberately — this window embeds the harness's own Mesh/Map/Atlas routes, the source product embeds its own console routes",
    "apps/menubar/Sources/Data/ConfigPersistence.swift": "small atomic property-list persistence helper written fresh",
    "apps/menubar/Sources/Data/RowSectionPolicy.swift": "the panel section rule as a free function over plain values: the server's bucket decides, the status string is the fallback, and paused work is held beside running rather than queued as next; written fresh",
    "apps/menubar/Sources/Data/SystemTelemetryDisplayPolicy.swift": "standalone compact telemetry geometry, ordering, and threshold policy written fresh",
    "apps/menubar/Sources/UI/CoordBrandFont.swift": "native client source",
    "apps/menubar/Sources/UI/PremiumSystemTelemetryDetailView.swift": "written fresh: the dense Stats detail popover -- layout contract, module ordering, ring and battery geometry -- that the standalone dockable Stats status item opens",
    "apps/menubar/Sources/UI/SystemTelemetryDetailView.swift": "standalone AppKit telemetry detail renderer written fresh against the public telemetry model",
    "apps/menubar/Sources/UI/SystemTelemetryRow.swift": "standalone compact AppKit telemetry row written fresh against the public telemetry model",
    "apps/menubar/Sources/Usage/InstalledUsageDashboard.swift": "standalone menu-bar integration wrapper for the shared usage dashboard written fresh",
    "apps/project.yml": "project generation spec",
    "apps/uninstall.sh": "standalone reversible macOS uninstaller that preserves user state",
    "docs/README.md": "written fresh for this repository",
    "docs/agent-onboarding.md": "standalone agent-client onboarding and operating guide",
    "docs/agent-protocol.md": "written fresh for this repository",
    "docs/architecture.md": "written fresh for this repository",
    "docs/assets/README.md": "written fresh for this repository",
    "docs/assets/VISUAL_LANGUAGE.md": "written fresh for this repository",
    "docs/assets/architecture.svg": "hand-authored diagram asset",
    "docs/assets/birdseye.svg": "written fresh for this repository",
    "docs/assets/casts/proof-gated-done.json": "real, unedited captured terminal transcript from examples/proof-gated-done/run.py, the source data tools/render_terminal_cast.py renders",
    "docs/assets/context-retrieval.svg": "hand-authored diagram asset",
    "docs/assets/context-tiers.svg": "hand-authored diagram asset",
    "docs/assets/context.svg": "written fresh for this repository",
    "docs/assets/extraction.svg": "hand-authored diagram asset",
    "docs/assets/handoff-sequence.svg": "hand-authored five-panel handoff sequence from the standalone lifecycle implementation and smoke test",
    "docs/assets/handoff.svg": "hand-authored diagram asset",
    "docs/assets/jobs.svg": "written fresh for this repository",
    "docs/assets/lifecycle-proof.svg": "hand-authored diagram asset",
    "docs/assets/lifecycle.svg": "deterministically generated from coord_db.py by the locally authored lifecycle diagram tool",
    "docs/assets/multi-agent-jobs.svg": "hand-authored diagram asset",
    "docs/assets/projection-topology.svg": "hand-authored diagram asset",
    "docs/assets/proof-gated-done.svg": "deterministic animated SVG rendered by tools/render_terminal_cast.py from docs/assets/casts/proof-gated-done.json",
    "docs/assets/provenance.json": "records how each diagram asset was produced",
    "docs/assets/screens/board-activity.png": "screenshot of the shipped dashboard on seeded demo data",
    "docs/assets/screens/board-graph.png": "screenshot of the shipped dashboard on seeded demo data",
    "docs/assets/screens/board-jobs.png": "screenshot of the shipped dashboard on seeded demo data",
    "docs/assets/screens/board-overview.png": "screenshot of the shipped dashboard on seeded demo data",
    "docs/assets/screens/board-work.png": "screenshot of the shipped dashboard on seeded demo data",
    "docs/assets/screens/ios-home.png": "clean-room native iOS capture; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/screens/macos-cockpit.png": "native client capture on the synthetic demo board",
    "docs/assets/screens/macos-panel.png": "native client capture on the synthetic demo board",
    "docs/assets/screens/map-ceiling.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-chronicle.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-context.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-crossings.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-deps.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-drawer.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-fleet.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-flowpath.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-orbit.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-pulse.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-search.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-shape.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-subjects.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/map-topology.png": "coordination map capture from the synthetic board",
    "docs/assets/screens/operations-atlas-overview.png": "Operations Atlas overview capture from the synthetic board",
    "docs/assets/screens/operations-atlas-topology.png": "Operations Atlas topology capture from the synthetic board",
    "docs/assets/screens/swarm-mesh-context.png": "Swarm Mesh Context-lens capture from the synthetic demo board; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/screens/swarm-mesh-critical.png": "Swarm Mesh Critical Flow capture from the synthetic demo board; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/screens/swarm-mesh-mobile.png": "Swarm Mesh responsive mobile capture from the synthetic demo board; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/screens/swarm-mesh-owners.png": "Swarm Mesh actor-lane capture from the synthetic demo board; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/screens/swarm-mesh-traversal.png": "Swarm Mesh traversal capture from the synthetic demo board; exact provenance is declared in docs/assets/provenance.json",
    "docs/assets/single-authority-flow.svg": "hand-authored diagram asset",
    "docs/assets/system-architecture.svg": "hand-authored diagram asset",
    "docs/claude-code-plugin.md": "docs page for the shipped Claude Code plugin integration (single-plugin marketplace, skill and command packages)",
    "docs/codex-prompts.md": "self-contained prompts for the visual and graph work still outstanding",
    "docs/comparison.md": "docs page contrasting COORD's narrow claim against adjacent orchestration frameworks, agent runtimes, and task trackers",
    "docs/compatibility.md": "written fresh for this repository",
    "docs/component-map.md": "the full component/family map",
    "docs/containers.md": "docs page for the devcontainer and plain docker build/run paths that mirror the native CLI walkthrough",
    "docs/context-and-memory.md": "standalone context and memory operator guide written fresh for this repository",
    "docs/context-architecture.md": "written fresh for this repository",
    "docs/coordination-model.md": "written fresh for this repository",
    "docs/design-notes.md": "decisions and their reasoning",
    "docs/diagrams.md": "mermaid diagrams of the mechanisms",
    "docs/extraction.md": "written fresh for this repository",
    "docs/feature-status.json": "Machine-readable feature status table, with the status vocabulary it uses.",
    "docs/friend-acceptance.md": "clean-room automated and human friend acceptance checklist written fresh",
    "docs/getting-started.md": "Getting-started guide: create a board, claim one proof-gated item, read the result.",
    "docs/governance.md": "written fresh for this repository",
    "docs/graph-and-context.md": "written fresh for this repository",
    "docs/ideas.md": "open problems and directions",
    "docs/jobs-and-runs.md": "written fresh for this repository",
    "docs/local-models.md": "local model operation guide, written fresh",
    "docs/mcp-integration.md": "written fresh for this repository",
    "docs/mcp-server.md": "written fresh for this repository",
    "docs/module-coverage.md": "public module coverage guide, written fresh",
    "docs/multi-agent-patterns.md": "written fresh for this repository",
    "docs/native-clients.md": "written fresh for this repository",
    "docs/next-steps.md": "the working queue: what is blocked, ranked engineering work, and what publication needs",
    "docs/operations-atlas.md": "world-class read-only Operations Atlas contract and operator guide written fresh",
    "docs/operations-graph-assessment.md": "sanitized read-only assessment written fresh before any operations graph port",
    "docs/operators-handbook.md": "standalone operator handbook written fresh for this repository",
    "docs/policy-pipeline.md": "written fresh for this repository",
    "docs/readme-redesign-proposal.md": "critical front-door information-architecture and visual-selection review copy written fresh",
    "docs/releasing.md": "written fresh for this repository",
    "docs/review-tiers.md": "written fresh for this repository",
    "docs/roadmap.md": "written fresh for this repository",
    "docs/safety-doctor.md": "public safety doctor guide, written fresh",
    "docs/schema.md": "written fresh for this repository",
    "docs/security-and-privacy.md": "trust model and publication rules",
    "docs/skills.md": "public operator skills guide, written fresh",
    "docs/standalone-setup.md": "single-path macOS setup, verification, and uninstall guide",
    "docs/static-demo.md": "docs page for the installer-free static demo board export built by tools/export_static_board.py",
    "docs/swarm-mesh.md": "source-bound Swarm Mesh rendering, motion, interaction, accessibility, and truth contract written fresh",
    "docs/third-party-notices.md": "written fresh for this repository",
    "docs/threat-model.md": "the untrusted-actor guard audit: which of today's guards would still do work, become redundant, or prove decorative under real authentication",
    "docs/usage-interface-parity.md": "Parity contract between the public usage surface and the private companion app - synthetic-fixture and no-private-data rules included (cross-lane review by claude post-merge)",
    "docs/visual-atlas.md": "index of the diagram assets",
    "docs/web-board.md": "written fresh for this repository",
    "examples/fleet-fan-out/README.md": "worked example: a fanned-out subagent history is recorded under one held claim, and refused when the claim is not held",
    "examples/fleet-fan-out/captured_output.txt": "real, unedited captured stdout of examples/fleet-fan-out/run.py backing the fleet-fan-out example README",
    "examples/fleet-fan-out/run.py": "runnable recipe proving record_child_attempt raises UnclaimedFleetError against a claim nobody holds, against a throwaway repo and coord.db",
    "examples/proof-gated-done/README.md": "worked example: coord done is refused until the declared artifact exists AND is staged in git's index, not merely on disk",
    "examples/proof-gated-done/captured_output.txt": "real, unedited captured stdout of examples/proof-gated-done/run.py; source data for docs/assets/casts/proof-gated-done.json",
    "examples/proof-gated-done/run.py": "runnable recipe proving coord done's completion gate against a throwaway repo and coord.db",
    "examples/two-agents-one-file/README.md": "worked example: two independent claims with overlapping write sets surface the collision by name, without blocking either claim",
    "examples/two-agents-one-file/captured_output.txt": "real, unedited captured stdout of examples/two-agents-one-file/run.py backing the two-agents-one-file example README",
    "examples/two-agents-one-file/run.py": "runnable recipe proving declare_write_set / write_set_overlaps reports an overlapping declaration against a throwaway repo and coord.db",
    "pyproject.toml": "written fresh for this repository",
    "scripts/check.sh": "local runner for the checks CI performs, in CI's order; refuses to report an extraction-gate result on unstaged changes and refuses to pass with the browser suite skipping for a missing dependency",
    "scripts/coord-mcp-launch.sh": "MCP launcher for a fresh clone: execs the clone's own venv Python, or fails closed with a plain setup instruction",
    "scripts/demo.sh": "written fresh for this repository",
    "scripts/friend_acceptance.sh": "isolated synthetic install, MCP, service, native-build, and data-preserving uninstall acceptance runner written fresh",
    "scripts/native_context_bridge.py": "read-only bridge answering the native cockpit's context palette",
    "scripts/route_plan_snapshot.py": "CLI wrapper, written fresh, building a non-mutating RoutePlanV1 from an explicit JSON snapshot through the pure assignment_routing planner; no live telemetry adapter, never opens coord.db, never imports a lifecycle writer -- every decision timestamp and evidence value comes from the caller",
    "scripts/setup-macos.sh": "idempotent standalone macOS bootstrap wrapper",
    "scripts/setup.sh": "OS-agnostic one-command clone setup: venv, coord.db, generated client config, and an opt-in native macOS/iOS install lane",
    "src/coordharness/__init__.py": "Package root; its docstring states what this control plane is and is not.",
    "src/coordharness/board/__init__.py": "written fresh for this repository",
    "src/coordharness/board/action_registry.py": "typed preview-only action declarations and check-by-check availability resolver written fresh",
    "src/coordharness/board/action_registry_v1.schema.json": "declared metadata for the preview-only action registry",
    "src/coordharness/board/graph_envelope.py": "bounded graph accounting and collision quarantine written fresh",
    "src/coordharness/board/graph_envelope_v1.schema.json": "declared metadata for the bounded graph envelope",
    "src/coordharness/board/local_system_telemetry.py": "dependency-free bounded local CPU GPU RAM and disk telemetry collector written for standalone COORD",
    "src/coordharness/board/native_snapshot_v1.schema.json": "JSON Schema for NativeSnapshotV1, the read-only snapshot the web and native surfaces share.",
    "src/coordharness/board/operations.py": "pure operational graph analytics written fresh from public board documents",
    "src/coordharness/board/ops_atlas_bundle_v1.schema.json": "declared metadata for one-generation atlas reads",
    "src/coordharness/board/ops_atlas_bundle_v2.schema.json": "declared metadata for the consolidated read-only communications and topology bundle",
    "src/coordharness/board/ops_atlas_v1.schema.json": "declared metadata for operational analytics",
    "src/coordharness/board/read_status_v1.schema.json": "declared metadata for redacted cache health",
    "src/coordharness/board/semantic_query.py": "canonical inspectable semantic Board query and display-state contracts written fresh",
    "src/coordharness/board/semantic_query_response_v1.schema.json": "declared metadata for semantic Board query results and omission receipts",
    "src/coordharness/board/semantic_query_v1.schema.json": "declared metadata for semantic Board query ASTs",
    "src/coordharness/board/server.py": "written fresh for this repository",
    "src/coordharness/board/snapshot.py": "written fresh for this repository",
    "src/coordharness/board/static/accent.js": "accent hue preference shared by the board and the map",
    "src/coordharness/board/static/app.css": "written fresh for this repository",
    "src/coordharness/board/static/app.js": "written fresh for this repository",
    "src/coordharness/board/static/brand/README.md": "how the optional licensed brand font is supplied",
    "src/coordharness/board/static/cockpit.css": "coordination map styling",
    "src/coordharness/board/static/cockpit.html": "coordination map page embedded by the native cockpit",
    "src/coordharness/board/static/cockpit.js": "coordination map: fleet matrix, dependency graph, context reader",
    "src/coordharness/board/static/comms-board.css": "standalone continuous communications workspace styling written fresh",
    "src/coordharness/board/static/index.html": "self-contained dashboard page",
    "src/coordharness/board/static/map-readability.css": "final coordination-map typography and control-size floor written fresh",
    "src/coordharness/board/static/mark-accelerator.png": "owner mark served to the web board",
    "src/coordharness/board/static/mark-claude.png": "owner mark served to the web board",
    "src/coordharness/board/static/mark-codex.png": "owner mark served to the web board",
    "src/coordharness/board/static/mark-compute.png": "owner mark served to the web board",
    "src/coordharness/board/static/motion.css": "shared motion layer",
    "src/coordharness/board/static/motion.js": "shared motion layer",
    "src/coordharness/board/static/neighbourhood.js": "dependency-free graph neighbourhood filter with explicit omission receipts",
    "src/coordharness/board/static/ops-atlas-model.js": "deterministic browser graph model and bounded topology written fresh",
    "src/coordharness/board/static/ops-atlas.css": "token-driven Operations Atlas styling and evidence motion",
    "src/coordharness/board/static/ops-atlas.html": "self-contained read-only Operations Atlas page",
    "src/coordharness/board/static/ops-atlas.js": "atomic read, structured questions, inspection, and evidence-bearing motion renderer",
    "src/coordharness/board/static/search.css": "token-only global row search styling",
    "src/coordharness/board/static/search.js": "dependency-free ranked global row navigation",
    "src/coordharness/board/static/shell.css": "shared product shell chrome",
    "src/coordharness/board/static/shell.js": "reads the embedded flag so a native host draws the chrome once, and declares the one destination model every board surface navigates by",
    "src/coordharness/board/static/swarm-mesh-model.js": "deterministic pseudo-3D cluster, occurrence-identity, filtering, traversal, and question model written fresh",
    "src/coordharness/board/static/swarm-mesh.css": "token-driven responsive Swarm Mesh control-room styling written fresh",
    "src/coordharness/board/static/swarm-mesh.html": "CSP-safe dependency-free Swarm Mesh control-room page written fresh",
    "src/coordharness/board/static/swarm-mesh.js": "Canvas topology renderer, coherent refresh, interaction, typed motion, and telemetry controller written fresh",
    "src/coordharness/board/static/timeline.css": "token-only row timeline styling",
    "src/coordharness/board/static/timeline.js": "read-only occurrence timeline renderer for the coordination drawer",
    "src/coordharness/board/static/view-ceiling.css": "board view module",
    "src/coordharness/board/static/view-ceiling.js": "board view module",
    "src/coordharness/board/static/view-chronicle.css": "Served stylesheet for the chronicle board view; tokens only, never injected inline.",
    "src/coordharness/board/static/view-chronicle.js": "Order: recorded moments across rows, and precedence crossed with stage",
    "src/coordharness/board/static/view-density.css": "Served stylesheet for the density board view; tokens only, never injected inline.",
    "src/coordharness/board/static/view-density.js": "Shape: work and state distribution across verticals",
    "src/coordharness/board/static/view-flow.css": "Served stylesheet for the flow board view; tokens only, never injected inline.",
    "src/coordharness/board/static/view-flow.js": "Crossings: dependencies aggregated to vertical level",
    "src/coordharness/board/static/view-flowpath.css": "board view module",
    "src/coordharness/board/static/view-flowpath.js": "board view module",
    "src/coordharness/board/static/view-orbit.css": "Served stylesheet for the orbit board view; tokens only, never injected inline.",
    "src/coordharness/board/static/view-orbit.js": "Orbit: one row and everything related to it at increasing distance",
    "src/coordharness/board/static/view-pulse.css": "board view module",
    "src/coordharness/board/static/view-pulse.js": "board view module",
    "src/coordharness/board/static/view-subjects.css": "Served stylesheet for the subjects (Territories) board view; tokens only, never injected inline.",
    "src/coordharness/board/static/view-subjects.js": "Subjects: how work distributes and relates across subject areas",
    "src/coordharness/board/static/view-topology.css": "board view module",
    "src/coordharness/board/static/view-topology.js": "board view module",
    "src/coordharness/board/static/wordmark.png": "wordmark served to the web board",
    "src/coordharness/board/system_telemetry_proxy.py": "bounded opt-in loopback telemetry adapter independently implemented for the standalone board",
    "src/coordharness/bootstrap.py": "written fresh for this repository",
    "src/coordharness/config.py": "fresh replacement for the source project's path table",
    "src/coordharness/coord/assignment_routing.py": "pure, evidence-bound assignment-routing planner written fresh: immutable snapshots in, hard exclusions evaluated, an expiring recommendation receipt out; never opens a database, claims work, or starts a run",
    "src/coordharness/coord/board_format.py": "human-readable table rendering for coord board at a real terminal; the JSON path (scripts, --json) is untouched by this module",
    "src/coordharness/coord/migrations/004_host_identity.sql": "migration record for the additive host_id columns on runs/agent_sessions, documenting why the ALTER itself lives in create_schema.py",
    "src/coordharness/coord/model_cli.py": "standalone model control CLI, written fresh",
    "src/coordharness/coord/onboarding.py": "standalone configuration generator and read-only onboarding doctor",
    "src/coordharness/coord/panel_quorum.py": "panel quorum: N independent verdicts that must agree, for irreversible work",
    "src/coordharness/coord/policy/__init__.py": "Package marker for the policy checks applied at every coordination boundary.",
    "src/coordharness/coord/runners/__init__.py": "Package marker for the runners that execute work outside an agent's own turn.",
    "src/coordharness/demo.py": "synthetic demo board; no real data",
    "src/coordharness/entry.py": "CLI entry point: creates and migrates the database on first use, and delivers declared refusals as one line on stderr instead of a traceback",
    "src/coordharness/jobs/cli.py": "written fresh for this repository",
    "src/coordharness/jobs/resource_lock.py": "standalone job resource lock, written fresh",
    "src/coordharness/jobs/roadmap_binding.py": "standalone tracked-launch authority binding, written fresh",
    "src/coordharness/knowledge/proposal_producer.py": "session-close producer for the memory-proposal queue",
    "src/coordharness/knowledge/read_surface.py": "bounded standalone knowledge read surface written fresh for this repository",
    "src/coordharness/lints/stall_detector.py": "conservative lint recognizing an agent-spawned child (subagent, workflow, background job) left untracked on the board",
    "src/coordharness/safety/__init__.py": "standalone safety package marker, written fresh",
    "src/coordharness/safety/doctor.py": "standalone safety diagnostics, written fresh",
    "src/coordharness/safety/git_guard.py": "standalone Git safety guard, written fresh",
    "src/coordharness/safety/mcp.py": "standalone safety MCP adapter, written fresh",
    "src/coordharness/safety/paths.py": "standalone safe-path primitives, written fresh",
    "src/coordharness/safety/writers.py": "standalone guarded writer primitives, written fresh",
    "src/coordharness/usage/local_account_actions.py": "local provider-account actions with bounded schemas and no external companion application dependency",
    "src/coordharness/usage/local_cache.py": "atomic clone-local last-good usage cache",
    "src/coordharness/usage/local_cost_cache.py": "Bounded read-only projection of local cost caches: symlink/size refusals, 20s jq subprocess with output cap and kill, strict JSON, per-row validation, fail-closed empty import (cross-lane review by claude post-merge)",
    "src/coordharness/usage/local_history.py": "bounded read-only import of current-user Claude and Codex CLI histories",
    "src/coordharness/usage/local_profiles.py": "local multi-account profile store with redacted public projections",
    "src/coordharness/usage/local_provider_management.py": "standalone provider account management over current-user CLI authentication",
    "src/coordharness/usage/local_routing.py": "standalone usage-aware routing projection over local provider state",
    "src/coordharness/usage/local_service.py": "standalone current-user provider account quota and history service",
    "src/coordharness/usage/routing.py": "usage-aware routing advice over the existing ledger; reads only, never switches",
    "src/coordharness/util.py": "small shared helpers; replaces one function from an unported package",
    "tests/extract/test_gate.py": "adversarial publication-gate regression tests",
    "tests/extract/test_port.py": "adversarial porter safety and atomicity regression tests",
    "tests/extract/test_repin.py": "tests for the authored.json completeness check: planted undeclared file, planted stale pin, refusal to invent a description or an unreasoned re-pin",
    "tests/jobs/test_launch_authority.py": "adversarial standalone tracked-launch authority tests",
    "tests/jobs/test_sidecar_writer_runtime.py": "standalone sidecar runtime regression tests",
    "tests/jobs/test_telemetry_redaction.py": "standalone telemetry redaction regression tests",
    "tests/knowledge/test_knowledge_read_surface.py": "standalone bounded knowledge read-surface regression tests",
    "tests/knowledge/test_memory_proposal_producer.py": "producer behaviour, including that it cannot strand a close",
    "tests/lints/test_apfs_cow_cleanroom.py": "regressions for the APFS copy-on-write clean-room test helper (coordharness.testing.apfs_cow_cleanroom)",
    "tests/lints/test_cwd_relative_paths.py": "regressions for the cwd-relative-path source lint (coordharness.lints.cwd_relative_paths)",
    "tests/lints/test_doc_deletion_guard.py": "regressions for the protected-markdown deletion guard lint (coordharness.lints.doc_deletion_guard)",
    "tests/lints/test_docs_findability_index.py": "regressions for the docs findability index lint (coordharness.lints.docs_findability_index)",
    "tests/lints/test_fail_loud_patterns.py": "regressions for the fail-loud, no-silent-swallow source lint (coordharness.lints.fail_loud_patterns)",
    "tests/lints/test_mtime_into_figure_lint.py": "regressions for the lint against filesystem mtimes baked into committed figures (coordharness.lints.mtime_into_figure_lint)",
    "tests/lints/test_pytest_gate.py": "regressions for the pytest completion-receipt gate (coordharness.testing.pytest_gate)",
    "tests/lints/test_verified_artifact_skip.py": "regressions for the verified-artifact-skip test helper (coordharness.testing.verified_artifact_skip)",
    "tests/lints/test_work_item_lint.py": "regressions for the work-item shape lint (coordharness.lints.work_item_lint)",
    "tests/publication/test_publication_gate.py": "exact Git-object, secret-scan, remote-receipt, and signature regressions written fresh",
    "tests/safety/test_doctor_readonly_wrapper.py": "safety doctor read-only wrapper regression tests",
    "tests/safety/test_git_guard.py": "Git safety guard regression tests",
    "tests/safety/test_safety_doctor.py": "safety doctor regression tests",
    "tests/safety/test_shell_scripts_ship_runnable.py": "published shell-script interpreter line, index mode, and non-Darwin memory-gate refusal regressions written fresh",
    "tests/test_agent_commands.py": "regressions pinning the shipped agent slash commands against the live CLI subcommands and MCP tool catalogue, not a list kept in the test",
    "tests/test_agent_first_contact.py": "regressions for scripts/coord-mcp-launch.sh's fail-closed first-contact path and both tracked client configs invoking it",
    "tests/test_agent_onboarding.py": "fresh-clone onboarding configuration and doctor regression tests",
    "tests/test_assignment_routing.py": "regressions for the pure assignment_routing planner written fresh: routing-policy weights, hard exclusions, and the canonical_sha256 receipt hashing",
    "tests/test_blocked_release_is_reachable.py": "regressions for the documented blocked claim release, driven from the handbook text itself",
    "tests/test_board.py": "written fresh for this repository",
    "tests/test_board_action_registry.py": "preview-only action availability, safety veto, and human-reason contract tests written fresh",
    "tests/test_board_brand_configuration.py": "configurable board branding regressions written fresh: committed-byte identity for the unconfigured default, mark, sub-label and title substitution across all four pages, escaping of an operator-supplied name containing markup, blank-value fallback, rewrite-scope containment, and startup refusal of an unusable value",
    "tests/test_board_comms_contract.py": "single-page communications, fleet-first, and pulse ordering regressions written fresh",
    "tests/test_board_done_is_verified.py": "regressions holding the board to its documented rule that a job's self-reported done is verified against its declared artifact, with the accepted-when-proved ablation, written fresh",
    "tests/test_board_first_contact.py": "missing-database refusal, single-sourced destinations, owner-mark legend, contrast floor, and per-row exchange population regressions written fresh",
    "tests/test_board_first_contact_browser.py": "real-browser exchanges plane, honest empty states, owner legend, and empty-board copy regressions written fresh",
    "tests/test_board_graph_agrees_with_the_snapshot.py": "regressions holding the board snapshot and the graph to one derivation of a job's self-reported state, so an unproved done claim cannot be refused on one document and served on the other, written fresh",
    "tests/test_board_options_contract.py": "path-specific HTTP method advertisement and read-only route regressions written fresh",
    "tests/test_board_palette_browser.py": "real-browser active-palette, ablation, and global-search keyboard regression tests",
    "tests/test_board_query_action_routes.py": "GET-only semantic query and action registry route integration and ablation tests written fresh",
    "tests/test_board_readability_browser.py": "responsive board typography, overflow, empty-state, and detail continuity browser regressions written fresh",
    "tests/test_board_readonly.py": "adversarial proof that every board route remains a coherent read-only projection",
    "tests/test_board_rows_are_a_surface_not_a_dump.py": "regressions holding /api/v1/snapshot to an operator surface rather than a dump of the work table: archived, superseded and closed rows, unranked queued and planned backlog, and the done tail older than a day are filtered out of rows; stuck rows age off the surface at fourteen days; summary keeps counting the whole board so the picture is always a stated subset of the census; and local job rows are never filtered whatever state they report; written fresh",
    "tests/test_board_rows_bounded.py": "regressions pinning the bounded, chunked board-row scan to produce identical rows, order, and contents as an unbounded Python filter",
    "tests/test_board_semantic_query.py": "canonical query, predicate, display separation, performance-shape, and omission receipt tests written fresh",
    "tests/test_board_server_ux.py": "regressions for coord-board's startup failure messages: a held port and a --db file with no board tables",
    "tests/test_board_session_identity.py": "board rows naming the holding session, not only the lane, written fresh",
    "tests/test_board_shell_coherence_browser.py": "coherent bundle refresh, stale-generation, command-palette, and mobile-shell browser regressions written fresh",
    "tests/test_board_table.py": "regressions for the terminal board table (grouped headers, lease-remaining column) versus the byte-unchanged JSON output",
    "tests/test_board_telemetry_source_binding.py": "regressions binding board job telemetry to the served database's own state root, with the default-layout and private-copy ablations, written fresh",
    "tests/test_board_transient_panels_browser.py": "transient panel dismissal and focus browser regressions written fresh",
    "tests/test_board_work_layout_browser.py": "board work-layout density and responsive hierarchy browser regressions written fresh",
    "tests/test_claim_admission_guards.py": "claim-admission regression tests written fresh",
    "tests/test_claim_holder_authority.py": "claim-ownership regressions written fresh: the database and MCP refusals, the holder's unchanged path, and the reaper/handoff/session-family callers that are meant to touch a claim they do not hold",
    "tests/test_claim_readiness_parity.py": "regressions for the shared MCP/CLI claim-readiness definition and the COORD_CLAIM_STRICT refuse/warn modes",
    "tests/test_cli_db_flag.py": "regressions for --db working after the subcommand name on every subcommand that reads the database, not only before it",
    "tests/test_cli_error_boundary.py": "regressions for the CLI error boundary: refusals as one line on stderr, the unknown-work-id message, --traceback, and unanticipated errors still crashing",
    "tests/test_cli_first_run.py": "Tests that a newcomer's first refusals name the next command: bare invocation, an empty board, and lifecycle verbs run out of order.",
    "tests/test_cli_review_verbs.py": "coverage for the CLI review surface (request-audit, verdict)",
    "tests/test_comms_mesh_atlas_browser.py": "continuous communications, fleet, pulse, mesh, and atlas integration browser regressions written fresh",
    "tests/test_completion_custody_scope.py": "regressions for done_signal_custodied's git-custody completion gate, scoped to Markdown proofs only",
    "tests/test_completion_gate_refusals.py": "completion-gate refusal regression tests written fresh",
    "tests/test_config_sentinels.py": "regressions for the sentinel-table check that decides which SQLite files are allowed to open as a coord.db",
    "tests/test_configured_lanes.py": "regressions proving COORD_LANES makes the agent lanes a deployment decision rather than a hardcoded claude/codex pair",
    "tests/test_continuation_outlives_the_lease.py": "regressions proving auto-requeue still fires after the blocking claim's lease expires, with the still-held and other-holder branches, written fresh",
    "tests/test_coord_inbox.py": "directed/broadcast inbox split and newest-window acknowledgement regressions written fresh",
    "tests/test_coordination_map_dependencies_focus_browser.py": "dependency-focus map interaction browser regressions written fresh",
    "tests/test_coordination_map_embedded_chrome_browser.py": "embedded map chrome de-duplication browser regressions written fresh",
    "tests/test_coordination_map_live_stylesheet_browser.py": "live coordination-map stylesheet and computed-style browser regressions written fresh",
    "tests/test_coordination_map_mode_menu_browser.py": "coordination-map mode menu browser regressions written fresh",
    "tests/test_coordination_map_shell_browser.py": "coordination-map deep-link, omission, refresh-fence, and zero-topology browser regressions written fresh",
    "tests/test_coordination_map_typography_browser.py": "computed-style coordination-map type and control floor regressions written fresh",
    "tests/test_create_is_not_an_upsert.py": "regressions proving `coord create` is an insert and not an upsert: the racing-create refusal, the in-transaction collision, and the amend path it must not break, written fresh",
    "tests/test_db_file_modes.py": "regressions pinning the measured file mode of coord.db and its SQLite WAL/SHM sidecars",
    "tests/test_declared_counts.py": "regressions pinning the README, docs/feature-status.json, and the server's live MCP tool catalogue to one AST-derived count",
    "tests/test_deferred_tool_promotion.py": "deferred-tool-promotion regression tests written fresh",
    "tests/test_demo_subcommand.py": "regressions for coord demo as a thin subparser over the existing coordharness.demo.seed module form",
    "tests/test_doctor_first_run.py": "regression coverage for the first-run doctor path",
    "tests/test_doctor_guidance.py": "regressions for coord doctor's per-finding machine-readable code and executable remediation, alongside the unchanged v1 shape",
    "tests/test_documentation_validation.py": "adversarial local-link, JSON, SVG accessibility, visual-provenance, and bounded-report regressions written fresh",
    "tests/test_done_claim_declares_its_proof.py": "regressions separating the three answers a done claim can get -- artifact resolved, artifact declared and missing, no artifact ever declared -- so a job that produced no proof stops being labelled as one whose proof was read, written fresh",
    "tests/test_entry_first_command.py": "first-command read-only entry point regressions written fresh",
    "tests/test_getting_started_reproducible_clock.py": "frozen-seed-clock lease expiry and getting-started accuracy regressions written fresh",
    "tests/test_graph_envelope.py": "adversarial graph omission, collision, cap, and determinism tests",
    "tests/test_host_identity.py": "regressions for the host_id columns, foreign-host pid liveness reporting, and hostname-rename tolerance",
    "tests/test_imports.py": "guards against dangling references to packages the extraction left behind",
    "tests/test_job_staleness_is_not_only_running.py": "regressions extending the job staleness check from `running` to every state that still owes an update, with the terminal-state cover that keeps finished work from warning forever, written fresh",
    "tests/test_lifecycle_diagram.py": "deterministic source-drift and unknown-status regression tests",
    "tests/test_lifecycle_smoke.py": "end-to-end proof of the lifecycle and its guards",
    "tests/test_lifecycle_writer_inventory.py": "lifecycle-writer inventory regression tests written fresh",
    "tests/test_liveness_hardening.py": "regressions for pid_start_time's locale forcing, the sidecar-index pid-start cross-check, and proc-pattern backtracking caps",
    "tests/test_local_system_telemetry.py": "bounded local system telemetry parser and cache regression tests",
    "tests/test_loop_contracts_verify_command.py": "regressions pinning the loop contract's verify command to a subcommand the package actually ships, and to the shared constant every emission site reads",
    "tests/test_mcp_generic_preflight.py": "fresh-database generic MCP preflight next-work and context regression tests",
    "tests/test_mcp_preflight_fact_store.py": "regression coverage for the MCP fact-store bootstrap",
    "tests/test_mcp_route_tool.py": "read-only MCP usage-routing tool coverage and coverage-refusal regressions written fresh",
    "tests/test_metrics_endpoint.py": "regressions for the /metrics route: reachability, Host/Origin guard parity, no caching, and valid Prometheus text exposition",
    "tests/test_mlx_runner_preflight.py": "local runner preflight regression tests",
    "tests/test_model_catalog_authority.py": "model catalog authority regression tests",
    "tests/test_model_productization.py": "model productization regression tests",
    "tests/test_native_cockpit_session_grouping.py": "Tests that cockpit rows group by orchestrating session as a dimension separate from the epic axis, which is pinned alongside it.",
    "tests/test_native_operator_writes.py": "regressions for the native-operator HTTP write path written fresh: default-off browser/native writes, an authenticated loopback transfer that is fenced and replay-safe, ambiguous-method and claim-conflict refusals, truthful projected action reasons, and the installer's explicit opt-in default",
    "tests/test_neutral_config_files.py": "neutral configuration publication tests",
    "tests/test_neutral_runtime_defaults.py": "neutral runtime default publication tests",
    "tests/test_no_hardcoded_endpoints.py": "standalone endpoint-neutrality regression tests",
    "tests/test_operator_ok_authority.py": "structural census of every writer that can insert an events row with a caller-chosen kind, pinning the agent-facing operator_ok surface",
    "tests/test_operator_sign_off.py": "operator review-gate sign-off regressions written fresh, including the directions an agent would take at it",
    "tests/test_operator_reassignment.py": "regressions for the typed operator-reassignment writer in coord_db.py written fresh: an exact system-handoff receipt, non-echoing operation-id collisions, invalid-authority/CAS refusals, never stealing a live claim, refusing completed or live work, and a public event writer unable to squat the reassignment namespace",
    "tests/test_operations_atlas.py": "operational graph analytics, cycle, and health regressions",
    "tests/test_operations_atlas_above_fold_browser.py": "laptop and mobile Atlas hierarchy, topology visibility, overflow, and complete-summary browser regressions written fresh",
    "tests/test_operations_atlas_attention_browser.py": "attention-state and bounded-neighbourhood Operations Atlas browser regressions written fresh",
    "tests/test_operations_atlas_browser.py": "browser truth, accessibility, motion, freeze, and responsive regressions",
    "tests/test_operations_atlas_mobile_affordance_browser.py": "mobile Operations Atlas affordance and overflow browser regressions written fresh",
    "tests/test_operations_atlas_url_selection_browser.py": "canonical Operations Atlas selection, hash restore, and omission browser regressions written fresh",
    "tests/test_operations_route.py": "atomic bundle, redacted health, and last-good-cache regressions",
    "tests/test_operations_schema.py": "Operations Atlas schema validation regressions",
    "tests/test_packaging.py": "guards defects that only appear once the package is installed",
    "tests/test_pages_workflow.py": "regressions checking the Pages workflow's build job never requests a Pages permission it was not granted",
    "tests/test_palette.py": "computed-style palette sweep, persistence, and CSSOM ablation browser tests",
    "tests/test_panel_quorum.py": "coverage for panel quorum",
    "tests/test_plugin_package.py": "structural regressions for the Claude Code plugin manifest pair: well-formed, version-consistent, no absolute path or personal identifier",
    "tests/test_policy_pipeline_resource_modes.py": "regressions asserting the policy pipeline and the config resolver agree on one resource-modes path, so the two cannot silently diverge again",
    "tests/test_portability_guards.py": "behavioral regressions for platform-gap handling in every module that touches fcntl or ps, simulating the capability's absence rather than string-matching source",
    "tests/test_privacy_hygiene.py": "opaque publication privacy gate regressions",
    "tests/test_product_runtime.py": "written fresh for this repository",
    "tests/test_prose_citations.py": "regressions checking every '[doc §heading](target)' citation resolves to a real heading",
    "tests/test_public_generalization.py": "public generalization regression tests",
    "tests/test_public_hygiene_sweep.py": "tracked-tree public-hygiene sweep regression tests written fresh",
    "tests/test_reaper_console_and_dry_run.py": "reaper console-script resolution and dry-run-vs-real-release regressions written fresh",
    "tests/test_reaper_console_script.py": "installed-wheel coord-reaper console-script resolution regressions written fresh",
    "tests/test_reaper_launchd_template.py": "opt-in reaper LaunchAgent template and install.sh flag regressions written fresh",
    "tests/test_release_claim_guard.py": "regressions for release_claim's epoch check and terminal-state guard on the paused/blocked branch",
    "tests/test_repo_citizenship.py": "root SECURITY.md/CONTRIBUTING.md/CODE_OF_CONDUCT.md citizenship regression tests written fresh",
    "tests/test_resume_trigger_contract.py": "resume-trigger contract regression tests written fresh",
    "tests/test_route_plan_snapshot.py": "regressions for scripts/route_plan_snapshot.py written fresh: its own snapshot schema and its behavior invoked as a subprocess",
    "tests/test_runtime_module.py": "regressions for coordharness.runtime's console-release retention, native-thread guard, and served-build-identity helpers",
    "tests/test_session_addressed_notes.py": "session-addressed note writer, lane-default preservation, and dead-target refusal regressions written fresh",
    "tests/test_setup_macos_client_registration.py": "opt-in MCP client registration gating regressions for the CLI-only setup path, written fresh",
    "tests/test_setup_script.py": "regressions for scripts/setup.sh (syntax, --dry-run's no-op guarantee, the setup-macos.sh shim, and the Python-floor refusal)",
    "tests/test_shared_shell_operations_atlas_label_browser.py": "shared shell and Operations Atlas naming coherence browser regressions written fresh",
    "tests/test_skill_packages.py": "paired public skill package regression tests",
    "tests/test_snapshot_readonly_wal.py": "read-only WAL snapshot safety regression tests",
    "tests/test_stall_detector.py": "regressions for the stall-detector lint's pure detect_stall core and its coord.db-facing scan_coord_db half",
    "tests/test_static_export.py": "regressions for the self-contained static board export bundle: expected files, synthetic-data disclosure, no absolute path leaked",
    "tests/test_swarm_mesh_browser.py": "real-browser spatial layout, truth-bound motion, interaction, responsive, and rollover regressions",
    "tests/test_swarm_mesh_embedded_height_browser.py": "embedded Swarm Mesh height and overflow browser regressions written fresh",
    "tests/test_swarm_mesh_low_topology_browser.py": "low-information Mesh admission, disabled-control, and no-motion browser regressions written fresh",
    "tests/test_swarm_mesh_route.py": "Swarm Mesh route, static asset, CSP, and read-only regression tests",
    "tests/test_system_telemetry_contract.py": "bounded system-telemetry proxy validation and loopback-policy regressions written fresh",
    "tests/test_terminal_cast.py": "regressions for tools/render_terminal_cast.py's reproducibility and accessibility, and that the shipped SVG matches its committed transcript today",
    "tests/test_testing_helpers.py": "regressions for the apfs_cow_cleanroom, pytest_gate, and verified_artifact_skip test helpers under coordharness.testing",
    "tests/test_uninstall_script.py": "regressions for apps/uninstall.sh: bash syntax, a real removal against a faked HOME/layout, and refusal on a foreign LaunchAgent Label",
    "tests/test_usage_account_action_route.py": "standalone usage account-action route authority and method regressions written fresh",
    "tests/test_usage_dashboard_browser.py": "provider usage dashboard behavior, responsiveness, and disclosure browser regressions written fresh",
    "tests/test_usage_dashboard_route.py": "provider usage route, cache, and schema-boundary regressions written fresh",
    "tests/test_usage_routing.py": "tests for the usage routing advisor",
    "tests/test_workflow_action_pins.py": "Tests that every workflow action is pinned to a commit, especially in the publish job that holds id-token: write.",
    "tests/test_write_set_declaration.py": "declared write-set reachability, schema bootstrap, read-only conflict query, and ungrantable-scope refusal regressions written fresh",
    "tests/usage/fixtures/claude_cost_cache.json": "Synthetic claude cost-cache fixture with a /private/must-not-cross sentinel proving per-project rows never cross (reviewed: no real values)",
    "tests/usage/fixtures/codex_cost_cache.json": "Synthetic codex cost-cache fixture, same must-not-cross sentinel (reviewed: no real values)",
    "tests/usage/fixtures/codex_model_attribution.jsonl": "Synthetic codex session log alternating model-alpha/model-beta turn contexts for attribution tests (reviewed: no real values)",
    "tests/usage/test_account_actions.py": "account-action forwarding, validation, and secret-redaction regressions written fresh",
    "tests/usage/test_dashboard_proxy.py": "bounded provider usage proxy validation, cache, and redaction regressions written fresh",
    "tests/usage/test_local_cache.py": "atomic local usage cache regression tests",
    "tests/usage/test_local_foundation.py": "standalone provider account history routing and management regression tests",
    "tests/usage/test_probe_platform_support.py": "unsupported-platform provider probe detection regressions written fresh",
    "tests/usage/test_provider_management.py": "provider-management forwarding and response-sanitization regressions written fresh",
    "tests/usage/test_repository_privacy.py": "repository-level provider state and credential publication regressions written fresh",
    "tools/capture_board_screens.py": "clean-room dashboard screenshot capture helper, written fresh",
    "tools/export_static_board.py": "exports the seeded demo board as a self-contained static, read-only site (index.html plus its JSON) needing no install or server",
    "tools/extract/merge_proposals.py": "merges agent-proposed port entries into the working manifest",
    "tools/extract/repin.py": "checks that authored.json declares every staged file and that every derived reviewed_sha256 matches staged content, and applies the mechanical half of the fix",
    "tools/extract/vocabulary.example.json": "generic rename/refusal template; the real one is gitignored",
    "tools/generate_lifecycle_diagram.py": "stdlib AST-based lifecycle SVG generator written fresh for this repository",
    "tools/privacy_hygiene.py": "opaque current-tree and reachable-history privacy gate",
    "tools/public_hygiene_sweep.py": "tracked-tree public-hygiene sweep for home paths, emails, work-id grammar and maintainer-supplied vocabulary, written fresh",
    "tools/publication_gate.py": "standalone exact-object local and signed-remote release decision gate written fresh",
    "tools/render_terminal_cast.py": "renders a captured terminal transcript (docs/assets/casts/*.json) into a byte-deterministic, accessible animated SVG",
    "tools/validate_documentation.py": "single read-only documentation and visual-provenance validator used by CI and local publication review"
  },
  "derived": {
    "apps/Shared/Sources/ProviderManagementView.swift": {
      "origin": "ported from a pinned private provider-account surface, then substantially adapted for the standalone harness",
      "reason": "Provider account presentation and concurrency-safe sanitized settings transport were reworked around the public harness endpoint and generic provider contract.",
      "reviewed_sha256": "c3f3795f63057f8a8b67ff4d40b330801fa85aa783e49eff7fe88a4494ff862f"
    },
    "apps/Shared/Sources/SystemTelemetry.swift": {
      "origin": "ported from a pinned private local-telemetry model, then substantially adapted for the standalone harness",
      "reason": "The public model keeps bounded CPU, GPU, memory, and disk telemetry while replacing private service assumptions with a generic opt-in endpoint contract. Re-reviewed (agent self-review, not independent human review) after the snapshot gained appBytes/wiredBytes/compressedBytes fields and a MemoryRingComposition type that reconciles App, Wired, Compressed, and Free segments to installed physical memory, keeping swap as metadata never drawn as a physical-memory slice; no network surface or identifier changed.",
      "reviewed_sha256": "8cba14aa0d6b03c6e5a30c58069e42c34bfb42b56718f0cffedbce5b412fae61"
    },
    "apps/Shared/Sources/SystemTelemetryView.swift": {
      "origin": "ported from a pinned private local-telemetry view, then substantially adapted for the standalone harness",
      "reason": "The compact shared telemetry strip was redesigned around the public system-telemetry snapshot and neutral severity presentation. Re-reviewed (agent self-review, not independent human review) after the four telemetry rows were reordered (RAM leads) and recolored, and the view body was split into expandedCockpitStrip/compactStrip helpers; presentation only, no new data source.",
      "reviewed_sha256": "d5c963fe1fd6715131b35bbeb8c3db42dc62fec0055a9d31a2b2930742dcd579"
    },
    "apps/Shared/Sources/UsageAccountActions.swift": {
      "origin": "split from a pinned private provider-usage client, then substantially adapted for the standalone harness",
      "reason": "User-triggered account actions were isolated behind a bounded public transport with explicit schema validation and redacted failures.",
      "reviewed_sha256": "c13953245025826b4db34af14985e9992f02d0e2847881f11c892f109b1068f9"
    },
    "apps/Shared/Sources/UsageAccountSettingsModel.swift": {
      "origin": "split from a pinned private provider-usage client, then substantially adapted for the standalone harness",
      "reason": "Account settings state and refresh behavior were separated from the source client and generalized for the public provider contract.",
      "reviewed_sha256": "835d7821516f5abd1ff9f06dd1d3485be0a5c7bac4165a608cac05edf75e9967"
    },
    "apps/Shared/Sources/UsageAccountSettingsView.swift": {
      "origin": "split from a pinned private provider-usage dashboard, then substantially adapted for the standalone harness",
      "reason": "Provider controls were extracted into a generic settings view with no private account state or product vocabulary.",
      "reviewed_sha256": "f073e89f2f479703c9bff4d24d00e729f25f4cb5b07fd5a66f9d2ee7faf1b239"
    },
    "apps/Shared/Sources/UsageDashboardContent.swift": {
      "origin": "split from a pinned private provider-usage dashboard, then substantially adapted for the standalone harness",
      "reason": "Compact and expanded usage presentation was modularized and generalized around the public provider-separated usage schema. Re-reviewed (agent self-review, not independent human review) after adding a per-provider visual style helper (asset name/tint), a totalTokensCost roll-up label, and an onExpandedChange callback replacing the prior @AppStorage-backed expansion state; no new endpoint or schema.\n\nRe-pinned: UsageWindowGeometry moved here from PopoverController and dashboard carries model breakdowns per plotted point (cross-lane review by claude post-merge) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: Reviewed responsive usage route prevents narrow-panel overflow, starts at the top, and clamps to the visible screen. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "ad9da5065d9e4d4ed92fd8cb78c3ce64430105bf8aa15dd4a368b6809d07145c",
      "repin": {
        "reason": "Reviewed responsive usage route prevents narrow-panel overflow, starts at the top, and clamps to the visible screen.",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "5e8eb31f674931bb4c07f79c4279b86f608650c2bd5fa2032aa4385e22cea217"
      }
    },
    "apps/Shared/Sources/UsageIntelligence.swift": {
      "origin": "combined from pinned private provider-usage model and client mechanisms, then substantially adapted for the standalone harness",
      "reason": "Provider quota, history, cost, retention, and forecast models were consolidated behind a neutral public schema with bounded decoding and no private ledger bytes. Re-reviewed (agent self-review, not independent human review) after UsageDaily gained an optional modelBreakdowns array (UsageDailyModelBreakdown: per-model token and cost fields) and Auto mode's session/weekly selection was reworked to compare each window's own remaining percent instead of a fixed threshold order; decode-only additions, no new transport.\n\nRe-pinned: Read the pace-marker diff for this file: UsageQuotaPace gained two optional decode-only fields (marker_remaining_percent, marker_kind) and the status presentation replaced a fixed warning-threshold marker with a pace marker derived from those fields and clamped to 0-100. Decoding and presentation only; no new data source and no private ledger bytes. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: Trend points carry tokens+model breakdowns; currentDayPoint checks local then UTC day, matching the history writer's astimezone attribution (cross-lane review by claude post-merge) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "a2c1aa36921111e1b31a2696da659c0cc20e97a5775561601701f37092e3c52a",
      "repin": {
        "reason": "Trend points carry tokens+model breakdowns; currentDayPoint checks local then UTC day, matching the history writer's astimezone attribution (cross-lane review by claude post-merge)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "6193ad25d9c3ddc32b2093c153ff21bed91134fd6dbb4abe0d2fb8bf3314c88d"
      }
    },
    "apps/menubar/Sources/App/AppDelegate.swift": {
      "origin": "ported from a pinned private application delegate, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after wiring concurrency-safe public Settings persistence, dedicated Settings navigation, and deterministic return to the main usage and task panel without stale popover state. Re-reviewed again (agent self-review, not independent human review) after wiring a LocalPowerController, a separate battery status item and a dockable Stats status item/popover, and passing telemetryStore into CockpitWindowController; local machine telemetry only, no new remote endpoint.",
      "reviewed_sha256": "22195f05021deed64aacda6d1bf7226097fb44e8277d5de62d3962b87938969d"
    },
    "apps/menubar/Sources/App/Diagnostics.swift": {
      "origin": "ported from pinned private menu-bar diagnostics, then substantially adapted for the standalone harness",
      "reason": "Diagnostics were reduced to neutral harness state and updated for the standalone application lifecycle.",
      "reviewed_sha256": "e32c001ffa1edbdd1cde30586fe61472e662a7020c47df229a628504ae0cd255"
    },
    "apps/menubar/Sources/App/PopoverController.swift": {
      "origin": "ported from a pinned private popover controller, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after consolidating main, settings, provider-account, usage, and telemetry navigation into one state machine reset by every close and attach transition. Re-reviewed again (agent self-review, not independent human review) after a battery navigation destination and a UsageWindowGeometry sizing helper were added, and the controller began taking a LocalPowerController to track and push local battery/caffeine state into the content stack; local machine state only.\n\nRe-pinned: Usage window sizes host window and glass together from shared UsageWindowGeometry on the real anchor screen (cross-lane review by claude post-merge) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: Reviewed host-first edge-pinned installation prevents AppKit from double-applying panel resize deltas. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "5242df02cd4adfdb118b2082e063a1c5b260f24339bfffabb5af69272d599dfd",
      "repin": {
        "reason": "Reviewed host-first edge-pinned installation prevents AppKit from double-applying panel resize deltas.",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "532ddf65584bb25bb76f932e7cd4542f7b9e240a3d302262e03c2123f96565ba"
      }
    },
    "apps/menubar/Sources/App/StatusItemController.swift": {
      "origin": "ported from a pinned private status-item controller, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after adding explicit queued Settings navigation, compact provider and telemetry status items, coalesced refresh, and stable primary versus secondary click routing. Re-reviewed again (agent self-review, not independent human review) after the primary status item stopped rendering telemetry rings itself (Stats now owns a separate dockable item, avoiding duplicate metrics) and gained a batteryStatusItemVisible flag/callback; presentation and config-forwarding only.",
      "reviewed_sha256": "c5f69fc470f0e44de616aebeb0332d7f4bce3281da85df0c0b3354290fed1d5b"
    },
    "apps/menubar/Sources/Cockpit/Data/CockpitCapabilityInventorySource.swift": {
      "origin": "ported from a pinned private cockpit capability reader, then substantially adapted for the standalone harness",
      "reason": "Capability discovery now follows the public endpoint policy and neutral standalone surface inventory.",
      "reviewed_sha256": "0a65634ed3db1c70f91da44c30cd5467ebc474ef17960715127abc3821b7cf88"
    },
    "apps/menubar/Sources/Cockpit/Data/CockpitHTTPFallbackSource.swift": {
      "origin": "ported from a pinned private cockpit fallback reader, then substantially adapted for the standalone harness",
      "reason": "Fallback reads now latch declined routes and expose bounded neutral errors instead of repeatedly probing an unavailable integration. Re-reviewed (agent self-review, not independent human review) after row decoding gained workVersion, currentAssignee, assignment/claim identifiers, claim liveness, live run count, and native-operator-writes enablement/reason fields, plus ints()/strings() helpers mirroring the existing typed dictionary accessors; read-path field additions only.\n\nRe-pinned: Read the diff: the fallback row decoder passes two further optional string columns (session_group_key, session_group_label) through to the model using the same nullable string accessor as the fields beside them. Decode-only; no new request, endpoint, or parsing path. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "1986292c66249f1cea3eef3e1bde1bb7950a2c34153494ffd1738a1c7d4149af",
      "repin": {
        "reason": "Read the diff: the fallback row decoder passes two further optional string columns (session_group_key, session_group_label) through to the model using the same nullable string accessor as the fields beside them. Decode-only; no new request, endpoint, or parsing path.",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "a63cdaf148d4047767f543e9ec004d3e45dc5da44e36751e3aed473930cdfde8"
      }
    },
    "apps/menubar/Sources/Cockpit/Data/CockpitProjectionRefresher.swift": {
      "origin": "ported from a pinned private cockpit refresher, then substantially adapted for the standalone harness",
      "reason": "Projection refresh now coordinates the public standalone read model with bounded coalescing and route-decline behavior.",
      "reviewed_sha256": "a29aaea61f653baa3cb307b0c5d60ef7b3895d7dbeac29edc22d6a52458d402e"
    },
    "apps/menubar/Sources/Cockpit/Data/CoordSQLite.swift": {
      "origin": "ported from a pinned private cockpit database reader, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after reducing database selection to explicit COORD_DB or the installer-persisted exact path and failing closed when neither exists; no home or checkout fallback remains.",
      "reviewed_sha256": "c5eda878d13c285dc975c60e4b861fe3e1042fa632f663e29f2510d99debefab"
    },
    "apps/menubar/Sources/Cockpit/Presentation/CockpitChromeLayout.swift": {
      "origin": "ported from a pinned private cockpit chrome layout, then substantially adapted for the standalone harness",
      "reason": "Compact navigation prioritization and responsive chrome geometry were rebuilt for the public cockpit surfaces.",
      "reviewed_sha256": "844e7b5854510729f7b0f5c36a88aaf502c1557dffa2df141c30ded167db7ac3"
    },
    "apps/menubar/Sources/Cockpit/UI/CockpitWindowController.swift": {
      "origin": "ported from a pinned private cockpit window controller, then substantially adapted for the standalone harness",
      "reason": "Window ownership now integrates shared standalone usage state, public navigation, and lifecycle-safe presentation. Re-reviewed (agent self-review, not independent human review) after the controller began requiring a telemetryStore passed through to CockpitRootView, calling rootView.prepareForPresentation() on show, and guarding the post-capability-action projection refresh on actionResult.ok so a failed action no longer triggers a refresh.",
      "reviewed_sha256": "ec778b42b9e6e88c282321e3b20cddea54884f02246daa5d0cf90667453e667a"
    },
    "apps/menubar/Sources/Data/Config.swift": {
      "origin": "ported from pinned private menu-bar configuration, then substantially adapted for the standalone harness",
      "reason": "Configuration now persists neutral endpoint, compact layout, usage palette, telemetry visibility, and threshold preferences. Re-reviewed (agent self-review, not independent human review) after batteryStatusItemEnabled was added and status-item/per-metric visibility decoding was moved behind a new MenuBarVisibilityPersistence(from:) helper, replacing direct per-key decodeIfPresent calls; same persisted keys, no new storage location.",
      "reviewed_sha256": "6aacb96e658dffdfeaf07ed6924cd3819adc5afb624e55b2ac65a8526820a9e2"
    },
    "apps/menubar/Sources/Data/HarnessClient.swift": {
      "origin": "ported from a pinned private status client, then substantially adapted for the standalone harness",
      "reason": "The client now uses the public endpoint policy, bounded timeouts, generic routes, and no private source-tree assumptions. Re-reviewed (agent self-review, not independent human review) after setMode and pauseAll gained an optional completion callback (HarnessControlOutcome) reported via a new postOutcome helper, so a caller can show verified success or failure instead of a fire-and-forget POST; same endpoints, same query parameters.\n\nRe-pinned: Row(cockpit:) carries the resolved session group key/label across; section stays the epic (agent self-review) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: bucket(for:) delegates to RowSectionPolicy and passes the server's bucket (lane) ahead of the group key (section); no other behaviour changed (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "9feeb8df907ad7dce947c410ae638cf8bbf1159f822e46519bfd4367315b6315",
      "repin": {
        "reason": "bucket(for:) delegates to RowSectionPolicy and passes the server's bucket (lane) ahead of the group key (section); no other behaviour changed",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "277544a7aa9d50c38d80ce590253b5503d199294010311fb0ac44997ea43b752"
      }
    },
    "apps/menubar/Sources/Data/Models.swift": {
      "origin": "ported from pinned private menu-bar models, then substantially adapted for the standalone harness",
      "reason": "The public models now carry standalone usage, telemetry, status, task, and configuration state without private product data.\n\nRe-pinned: Row gains optional sessionGroupKey/sessionGroupLabel carried from the projection; implicit nil keeps Codable and memberwise init compatible with older projections (agent self-review) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "81c68594384182fd69c5a3a3de3dbf1e6961ad16badd3827fc02456071301bc6",
      "repin": {
        "reason": "Row gains optional sessionGroupKey/sessionGroupLabel carried from the projection; implicit nil keeps Codable and memberwise init compatible with older projections (agent self-review)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "ef9be2a004dcfbf15788c07c66fe3d395ac2ced622031db71c63d106a97f57d6"
      }
    },
    "apps/menubar/Sources/UI/ContentStackAndRows.swift": {
      "origin": "ported from pinned private menu-bar row composition, then substantially adapted for the standalone harness",
      "reason": "The panel composition now integrates compact usage, telemetry disclosure, task rows, neutral marks, and the standalone navigation hierarchy. Re-reviewed (agent self-review, not independent human review) after PanelAction gained toggleCaffeine/openBatteryDetails/toggleChargeLimit cases and a CockpitWordmarkButton control was added binding the wordmark image to a press closure with an explicit accessibility label and help string.\n\nRe-pinned: Read the pace-marker diff for this file: the peek row now takes a paceMarkersVisible flag instead of a precomputed warning percent and draws each marker from that quota's own timing, red for a deficit and green otherwise. Layout and colour only; no new input reaches the panel. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: agentSessionKey prefers the projection's resolved chat key and falls through to the full previous derivation on an older projection (agent self-review) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "491b5cfc4cb67a78075e87f256462cb993ed9764ff181ba0f9340106b5523ee8",
      "repin": {
        "reason": "agentSessionKey prefers the projection's resolved chat key and falls through to the full previous derivation on an older projection (agent self-review)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "9897b132969e74e15e989dde158adabcdf9f0f61eff8ced38b5bf36f041c29e4"
      }
    },
    "apps/menubar/Sources/UI/RingRenderer.swift": {
      "origin": "ported from a pinned private progress renderer, then substantially adapted for the standalone harness",
      "reason": "The renderer now supports the public compact bar and ring geometry, palette choices, and deterministic status-item rendering. Re-reviewed (agent self-review, not independent human review) after drawTinyBar's warning-marker stroke color became a parameter instead of a hardcoded systemRed, and the warning severity color moved to a shared Tokens.Color.statsWarningOrange token; rendering-only, no new data source.\n\nRe-pinned: Read the pace-marker diff for this file: the status-item bar now draws a marker supplied by the presentation instead of one computed here from a threshold, and the palette-dependent warning-colour helper was deleted. Rendering only; the geometry and the 0-100 clamp are unchanged. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "0f633884eeeb76d5dddda5bd1afae000aef934af11fa23c01316993f99821434",
      "repin": {
        "reason": "Read the pace-marker diff for this file: the status-item bar now draws a marker supplied by the presentation instead of one computed here from a threshold, and the palette-dependent warning-colour helper was deleted. Rendering only; the geometry and the 0-100 clamp are unchanged.",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "7da5791c2fb85b9b0837f37230204b96e15039c99a8afb2f51d161ff88d7aaa4"
      }
    },
    "apps/menubar/Sources/UI/SettingsView.swift": {
      "origin": "ported from pinned private menu-bar settings, then substantially adapted for the standalone harness",
      "reason": "Settings were expanded and generalized for persistent usage, telemetry, layout, account, and standalone endpoint controls. Re-reviewed (agent self-review, not independent human review) after the view was restructured into a fixed-height shell with paged navigation (Page: General/Display/Usage/Power/Advanced) where only the selected page scrolls, plus onOpenPower/onOpenCockpit callbacks; still no private product vocabulary or account state.",
      "reviewed_sha256": "953940b39f29cedcba76e026dfe3e0ee193989bae50ee4372e9ce79f38367d84"
    },
    "scripts/set_mode.sh": {
      "origin": "ported, then extended in this repository",
      "reason": "The single sanctioned way to switch a machine-wide resource mode (full/medium/light/pause/auto): validates the mode name against resource_modes.json, atomically writes resource_mode.txt, drops a trigger file so a running governor daemon re-enforces within ~30s instead of waiting a full poll cycle, and supports a manual-override hold plus an idle+AC-power auto-resolve. This is the operator/dashboard-facing half of the 'resource-mode governor' contract. Re-reviewed after the file was made runnable: a shebang line and the execute bit in the index. Git shipped it mode 100644 with no interpreter line, so the invocation the operators' handbook documents failed with Permission denied in every clone. No change to what the script does.",
      "reviewed_sha256": "8b99777595c071bc82eba6f09174919d6551e3249e758acc4ccbbd112680ad71"
    },
    "src/coordharness/board/security.py": {
      "origin": "ported, then extended in this repository",
      "reason": "Pure, dependency-free Host/Origin guard (DNS-rebind/CSRF protection for a loopback-bound dashboard) plus an optional Starlette ASGI adapter, gated by a try/except ImportError so it degrades to None when starlette isn't installed. No product vocabulary, no product logic — this is exactly the kind of small stdlib-first primitive the harness board needs and it drops in with no edits.",
      "reviewed_sha256": "2486b8aec4281fd73072964d4ff14bbc547b2122b4f848ab4432ee9ae88dd13e"
    },
    "src/coordharness/board/static/usage-dashboard.css": {
      "origin": "ported from a pinned private provider-usage stylesheet, then substantially redesigned for the standalone board",
      "reason": "Provider-separated usage visuals were rebuilt with public board tokens, neutral harness naming, and compact geometry for one combined usage-system row.",
      "reviewed_sha256": "c21969ccda4fec490b7dcd975e3a59fb1a41f94338a30b7a9cc912d39df5da04"
    },
    "src/coordharness/board/static/usage-dashboard.js": {
      "origin": "ported from a pinned private provider-usage panel, then substantially adapted for the standalone board",
      "reason": "Re-reviewed after integrating the standalone provider document, compact local account actions, and one combined compact usage-system row while retaining bounded rendering and neutral schemas.",
      "reviewed_sha256": "8301f74a4dcfdc98e5e610a58a8ef0e1596828762a5b3e4c27634bae2dc23b1b"
    },
    "src/coordharness/coord/cli.py": {
      "origin": "ported, then extended in this repository",
      "reason": "Re-reviewed after adding standalone onboarding, generated client configuration, exact work-context reads, and typed fenced handoff commands; lifecycle writes remain explicit and database-scoped. Re-reviewed again after the CLI error-boundary pass: `--traceback` is declared for help text only, a claim collision is translated to a sentence naming the holder that the code reads back rather than infers, `work-context` now takes the not-found branch that `_work_row`'s empty-dict return had made unreachable, and `route` refuses a `--usage-db` path that is absent. No new write path and no change to any domain message. Re-reviewed after four client-surface additions: --write-scope on claim plus declare-write-set and conflicts, so the write-set overlap query the agent system prompt already instructs agents to use is reachable; owner session fields emitted beside the unchanged assignee; --to-session on note; and inbox binding to the reading session so a session-addressed note is visible from the shell. Existing output shapes gained fields and lost none. Re-reviewed after closing the claim-ownership hole: a claim_id was sufficient authority to mutate a claim held by another session, and the resulting row still attributed the action to the true holder. coord heartbeat-claim, release and done now pass this shell's resolved session identity to the coord_db mutators, so the CLI is covered by the same refusal as the MCP surface. No new command, no changed output shape. Re-reviewed after `coord create` stopped being an upsert: its existence check ran outside the write transaction and the write itself merged into whatever row it found, so two sessions choosing the same work id both reported \"created\": true and one silently overwrote the other. The verb now calls coord_db.create_work, which refuses a taken id from inside the transaction that inserts, and the emitted `created` is that call's measured result. No new command and no other changed output shape. Re-reviewed after `coord sign-off` and `coord release --reason` were added. sign-off is the only writer of operator_ok in the product and the one this file's `post_event` refusal has always named; it is deliberately absent from the MCP surface and gated on the invoking process's controlling terminal, read through /dev/tty rather than stdin, so a piped or scripted answer is never consulted. It resolves no lane identity and runs no lifecycle policy, because both read the ambient actor and an operator decision must not carry an agent's name. `release` gained `--reason`, the flag that reaches the `release_claim(reason=...)` parameter a blocked release has always required and this surface could not supply. No other command changed shape. Re-reviewed again after the durable-id generalization: the only change to this file is the work_id help example, which previously showed the extraction source's dated house convention and now shows the same generic shape the documentation teaches. Re-reviewed again (agent self-review, not independent human review) after `coord board` gained a human-readable table renderer (board_format.render_board_table) for a real terminal, subcommands began accepting `--db` after the verb via a shared `_add_subparser` helper reconciled in `_resolve_db_path`, `coord demo` was added to seed a disposable fictional board, and cross-lane verbs began requiring a configured counterpart lane via `_require_counterpart` (refusing rather than silently defaulting when only one lane is configured) instead of a hardcoded claude/codex pair; also fixes a sign-off terminal-confirmation bug that had refused every real terminal, by opening /dev/tty as two one-directional handles instead of one seek-required handle.\n\nRe-pinned: Read the diff: a bare invocation now prints a curated three-command pointer and returns the same exit code 2 argparse's own required=True produced, and the claim-id lookup distinguishes a work id passed where a claim id belongs from an unknown claim id. Message and control flow only; no query, schema, or lifecycle behaviour changed. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "6725b0c0f21f75b358ba906aa83504a577370f1f8a82bfcaa5571044dc358a95",
      "repin": {
        "reason": "Read the diff: a bare invocation now prints a curated three-command pointer and returns the same exit code 2 argparse's own required=True produced, and the claim-id lookup distinguishes a work id passed where a claim id belongs from an unknown claim id. Message and control flow only; no query, schema, or lifecycle behaviour changed.",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "2eaef6e127878b16c54658df4b9cd5c6affbd1c453c8a0a58917aeed833009dd"
      }
    },
    "src/coordharness/coord/coord_db.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.coord.coord_db). Re-reviewed after splitting the inbox into its directed and broadcast legs: `unread_inbox_counts` reports both plus their sum, `unread_inbox_count` now returns that sum so existing callers read the same figure, `read_inbox` labels every row `directed`, and its new `directed_only` argument is opt-in so the default reading returns the same SET of messages it always did. No new write path, no change to which events are recorded. Prior review of `post_note`, the `newest_first` option, and the session-close memory-proposal hook stands. Re-reviewed again after `claim_work` gained a pre-INSERT refusal for a work id that is not on the board: it replaces a foreign-key error with the id and where to look, cannot fire on the create-and-claim path that passes work_fields, and changes no other outcome. Re-reviewed again after `board_rows` began refusing a `group_by` that names no field on a work row: the old behaviour sorted every row into \"(ungrouped)\" and exited zero, so a typo and a real grouping were indistinguishable in the output. The refusal names the groupable fields. Read path only; no write path and no schema change. Re-reviewed after the session-granularity work: resolve_note_session_target validates a note's session target exists and is live and refuses a dead one; post_note accepts it with the lane default unchanged; board rows carry the owner session the v_work_owner view already computed. Also the successor pointer emitted at closeout now names a module a clone can run rather than a coordharness/scripts path no checkout contains. Re-reviewed after closing the claim-ownership hole: a claim_id was sufficient authority to mutate a claim held by another session, and the resulting row still attributed the action to the true holder. heartbeat_claim, release_claim and complete_claim now take the calling session's identity and refuse a caller that does not hold the claim, resolving the same session family the work_id path already resolves. CLAIM_MUTATION_SYSTEM_CALLERS is an enumerated door for the system paths (lease expiry, zombie reaping, typed handoff), each of which remains its own typed operation and passes nothing. No schema change. Re-reviewed after two coordination repairs. create_work is a new insert-or-refuse doorway for `coord create`, raising WorkIdCollisionError from inside the same transaction as its existence check; _upsert_work_unlocked gained an opt-in require_new flag and now returns whether it inserted, leaving the shared amend branch that fills fields on existing rows exactly as it was. apply_continuation_ready_transition now also accepts a requeue with no claim to release, for the blocked row whose claim lease expired and was released while the work item stayed sticky-blocked; it re-checks inside the transaction that no claim holds the row, so a row someone has taken is never requeued out from under them. No schema change. Re-reviewed after record_operator_sign_off was added: the typed human-only writer the public post_event refusal already named, and previously absent from the tree, leaving operator_ok schema, validators and consumers with nothing able to populate them. It mints the operator_ok event with actor=operator and trust=system and binds it to work_items.operator_ok_event_id under a compare-and-set in the same transaction, is replay-safe on an operation_id, and refuses a row with an open review barrier rather than recording a valid event that every reader then ignores. It writes no new column and changes no existing write path; post_event and upsert_work keep their refusals. The `operator-ok:` idempotency namespace is reserved in post_event alongside the two namespaces already reserved there, so a public writer cannot squat an operation id and turn a later real sign-off into a replay collision. Re-reviewed after widening completion custody beyond Markdown: complete_claim's proof refusal now names the artifact, the git add command, that the rule applies to every artifact type since 0.1.0, and the COORD_COMPLETION_CUSTODY_EXEMPT override (agent self-review, not independent human review).\n\nRe-pinned: board_rows tolerates a pre-host_id database: the missing column maps to the NULL pre-migration case pid_liveness_is_meaningful already documents, so a reader no longer refuses every DB its own older writers produced (agent self-review) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "59e942a204083a19dfc5620181dd80469ac8d17d33c81935154efb63570e4c92",
      "repin": {
        "reason": "board_rows tolerates a pre-host_id database: the missing column maps to the NULL pre-migration case pid_liveness_is_meaningful already documents, so a reader no longer refuses every DB its own older writers produced (agent self-review)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "b38776477a3a87048d16ab58256dba8bc544da38dbb13e0dbd41ff8c1bce5eea"
      }
    },
    "src/coordharness/coord/create_schema.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.coord.create_schema) Re-reviewed after the write-set tables joined the verified table inventory, so a bootstrap that silently omits them is caught rather than deferred to the first caller. Re-reviewed after the host_id columns (agent_sessions, runs) joined _EXPECTED_COLUMNS, so apply_schema raises rather than returns success when a column a multi-host deployment needs is missing; after _backfill_request_consumption's actor:claude/actor:codex CASE was generalized to the configured lane set via config.configured_lanes(), so the backfill still matches every lane's handoff/audit_request rows on a non-default COORD_LANES; and after enforce_db_file_modes() is now called on the resolved db path once the connection is closed, so the database's file mode -- which SQLite copies onto every WAL/SHM sidecar it creates afterward -- is fixed at the end of apply_schema rather than left to a caller.",
      "reviewed_sha256": "5aeb5811c675bcd8cd8eb702d288639be0be64e02c1faaf4d676be241b9acbfd"
    },
    "src/coordharness/coord/ingest.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.coord.ingest). Re-reviewed after resolve_identity's runner_type fallback was generalized from a hardcoded {'claude','codex','local'} membership test to config.lane_set() | {'local'}, so an identity whose actor names a lane added via COORD_LANES still reports that lane as its runner_type instead of falling back to the generic 'agent' label.\n\nRe-pinned: Claude registration derives worktree_id from git only for a LINKED worktree (primary worktree, missing git, unexpected shapes all yield NULL) - never from cwd, since a shared worktree_id forms a claim-authority family (agent self-review) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "5619266ab167de3e5068b0e214fc7e117e8e6407fc409be456cff09de044634b",
      "repin": {
        "reason": "Claude registration derives worktree_id from git only for a LINKED worktree (primary worktree, missing git, unexpected shapes all yield NULL) - never from cwd, since a shared worktree_id forms a claim-authority family (agent self-review)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "24a2c17327235b377958b26ab97ca97c686b7cc4055c36fd49bc75ff28c9d4b4"
      }
    },
    "src/coordharness/coord/locked_paths.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Portable strict-profile custody uses neutral local paths without assuming a product warehouse topology.",
      "reviewed_sha256": "f42b59f3dbc99e031ef6d594bb27d5e75dc19ba24c44b42a383a000a78c1d09b"
    },
    "src/coordharness/coord/mcp_coord_server.py": {
      "origin": "ported, then extended in this repository",
      "reason": "Re-reviewed after making preflight, next-work, work-context, knowledge reads, and typed handoff generic to the caller project and exact database rather than any private installation, and again after correcting the inbox tool: it now reads newest-first like the CLI, acknowledges the maximum event in the window it actually returned instead of the last list element, keeps an explicit backlog mode, and reports the directed/broadcast split plus what the acknowledgement swept past. Reads and one cursor write only. Re-reviewed after three tools were added -- declare_write_set, write_set_conflicts and a read-only route -- and note gained a session target. route passes the ledger's own coverage refusal through rather than re-deriving it, writes nothing, and refuses a usage database that does not exist instead of minting a second accounting store. The knowledge index_refresh command now names a callable that exists; it named a script absent from the repository. Re-reviewed after closing the claim-ownership hole: a claim_id was sufficient authority to mutate a claim held by another session, and the resulting row still attributed the action to the true holder. _resolve_lifecycle_claim_row now resolves the caller's identity on the claim_id branch unconditionally instead of only when the caller volunteered one, checks it against the claim holder, and returns it so every lifecycle verb forwards the asker rather than the stored holder to the database. Every pre-existing refusal on that branch is left in place; the tool signatures are unchanged. Re-reviewed after the hardcoded {'claude','codex'} pair was replaced everywhere in this module with the configured-lane helpers (config.configured_lanes/lane_set/counterpart_lane/lanes_display), so claim identity, typed handoff, request_audit, note, audit-kind reservation, audit_verdict's to_selector, and verdict's author lane all resolve against COORD_LANES instead of the two-lane default; each spot that used to derive 'the other of two lanes' inline now calls counterpart_lane and refuses with a named error when no second lane is configured, rather than silently computing a nonsense opposite. Re-reviewed again after _claim_quality_missing delegated to the single coord_db.claim_readiness definition (replacing an inline best-effort import of creation_lint.claim_rubric_missing_fields that silently returned no missing fields on any import error) and _tool_claim began honoring COORD_CLAIM_STRICT: a claim that fails readiness now either refuses via coord_db.ClaimReadinessError or proceeds with a claim_readiness warning payload, matching the CLI's own default-warn behavior instead of always refusing. Re-reviewed again after _coord_board_row started reading runs.host_id and skipping pid-liveness probes for a run recorded on a different host: probing a foreign pid asks the wrong kernel, so live_pid_count is now left unset (not zeroed) whenever any run on the row was recorded elsewhere, which keeps derive_work_status's lease/heartbeat fallback available instead of misreading a healthy foreign run as dead. Re-reviewed again after main() gained --help/--version handling, an explicit unknown-argument refusal to stderr with exit code 2, an argv parameter for testability, and a startup banner (version and resolved COORD_DB) written to stderr before mcp.run() -- no MCP tool, schema, or database behavior changed. Re-reviewed after the completion-custody widening: the stale-index refresh-and-retry path now asks custody_requires_tracking rather than testing for a .md suffix, so the MCP recovery path covers exactly what the gate covers (agent self-review, not independent human review).",
      "reviewed_sha256": "80e4514b86a7ae14ad2fdd9ec1413f82a719f20a05b25465b872af0b17c52f0d"
    },
    "src/coordharness/coord/modeld_lite.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Standalone model catalog, mode validation, backend preflight, request validation, planning, and execution extend the pinned mechanism.",
      "reviewed_sha256": "941192bde1fc99206d49cb69433867f7c800a19e2f4a8b7046b9e179ef0d96de"
    },
    "src/coordharness/coord/row_classification.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Standalone semantic-system validation and environment-configured product tokens replace source-specific classification tables.",
      "reviewed_sha256": "fe1d01557846287b21b0d3ec9522d153d1a2f23603f974eb9c37396909de1b6b"
    },
    "src/coordharness/coord/runners/mlx_runner.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Standalone local-model lifecycle preflight, fallback, escalation, generation, and cleanup materially extend the pinned runner. Re-reviewed after closing the claim-ownership hole: a claim_id was sufficient authority to mutate a claim held by another session, and the resulting row still attributed the action to the true holder. The runner passes its own session identity to the claim mutators it already called; behaviour is otherwise unchanged.",
      "reviewed_sha256": "a61d9b2db216111a64ad386fdd1f97322e0230567236d38b444d82ab3fc1e55c"
    },
    "src/coordharness/coord/work_contracts.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Write-scope exclusions use neutral state-directory paths rather than a deployment-specific runtime topology. Re-reviewed after the claim-required refusal stopped citing a section number in a private contract file and stated the rule itself instead. Message wording only; the refusal and its conditions are unchanged. Re-reviewed after the conflict query was made safe to call: read paths use a read-only helper instead of the lazy create, which raised 'attempt to write a readonly database' on any read-only connection for a table the caller never named. The report gained schema_present so an empty finding list cannot be read as proof of no conflicts when the query never ran.",
      "reviewed_sha256": "ed6780564ad5f14d0dec7ea957acc902af2aee147f7fe00df0f1995a126714a6"
    },
    "src/coordharness/jobs/diagnostic_marker.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.jobs.diagnostic_marker). Re-reviewed after the module-level `import fcntl` was moved behind a try/except ImportError (fcntl is POSIX-only) and wrapper_control_lock() gained an explicit WrapperControlLockError refusal when fcntl is unavailable: publish_wrapper_control()/_ensure_managed_sentinel() and sidecar_writer.py's callers depend on this lock to serialize concurrent wrapper launches for the same job_id, and there is no non-POSIX substitute, so the module now imports cleanly on a non-POSIX install and refuses only the one call path that cannot be made safe there, instead of failing at import time everywhere.",
      "reviewed_sha256": "a4784ff4c00c5dbe53ff8673abb525c927d0787f9db04935cac11cf7ef9de9ea"
    },
    "src/coordharness/jobs/launch.py": {
      "origin": "ported, then extended in this repository",
      "reason": "The Claude-facing 'wrap any command so it appears on the board the instant it starts and is marked done/failed the instant it exits' launcher, with a hard RSS cap on the whole child tree (default 3 GB) so a runaway job gets killed long before the >6GB watchdog would ever notice. Pure-stdlib process supervision (Popen(start_new_session=True), ps -Ao pid=,ppid=,rss= summed over descendants every ~1s) — this is the most directly reusable, generic deliverable of the whole subsystem: 'coord jobs launch --job-id X --work-id Y --cap-gb 3 -- <command>'. Re-reviewed after every os.killpg call site (_group_alive, _terminate_process_group, main's cap-kill path, the SIGTERM/SIGKILL signal helper) started calling a new _require_process_groups() guard first: POSIX_PROCESS_GROUPS_AVAILABLE is probed once at import time via hasattr(os, 'killpg'), and a platform without it now raises a named ProcessGroupUnsupportedError -- caught in main() and printed to stderr with exit code 2 -- instead of letting an unguarded call raise a bare AttributeError partway through supervising a child. No behavior changed on a platform that has os.killpg.",
      "reviewed_sha256": "165ea0f323d12c36d0795231686d47faf9a47a3b7c536bdeb127bcdbf4ee4fa1"
    },
    "src/coordharness/jobs/sidecar_snapshot.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.jobs.sidecar_snapshot)",
      "reviewed_sha256": "2d1c591fde0a4e04bb0c21cdb438fa1c65dbec3b229ab64054bd6285c381f5ae"
    },
    "src/coordharness/jobs/sidecar_writer.py": {
      "origin": "ported, then extended in this repository",
      "reason": "The canonical job_progress sidecar WRITER: atomic tempfile+fsync+os.replace writes, monotonic pct/done within a run, terminal-state fencing so a late write can't resurrect or flip a finished job, appear()-on-start / update() / finalize()-refuses-premature-done, a wrapper-compatible write path for a shell launcher, and reconcile_dead() — a fail-closed repair that only reclassifies a stale RUNNING sidecar to failed after proving every recorded process identity is provably absent via kill(0). This is the write-side half of the sidecar contract docs/jobs-and-runs.md already documents (only the read-side — status.py/sidecar_snapshot.py/diagnostic_marker.py — was ported before). Its diagnostic_marker.py dependency (imported here as job_authority) already exists at the destination with matching function names (wrapper_control_lock, observe_control, publish_wrapper_control, new_launch_id, normalize_launch_id, canonical_sidecar_path, set/clear_diagnostic_marker), so the bulk of this file ports mechanically. Re-reviewed after _pid_alive() and _prove_process_identity_absent() started refusing (raising ProcessLivenessUnsupportedError / DeadReconciliationError) rather than calling os.kill(pid, 0) or the negated-pid group probe when process_liveness.POSIX_LIVENESS_PROBE_AVAILABLE is false, so a platform where signal 0 does not mean a POSIX existence probe cannot be told a process is alive or provably absent when it is neither; and after both _update_locked and _wrapper_write_locked began (re)stamping pid_started_at via process_liveness.pid_start_time() whenever a sidecar's recorded pid changes (and only then, reusing the cached value on an unchanged pid), so a later PID reuse can still be told apart from the process this sidecar actually tracks. No change to the write contract, fencing, or the existing kill(0) behavior on a POSIX host with an unchanged pid.",
      "reviewed_sha256": "69250e8728efb8a3c443c5b9c10d4cfe263c0d5d1cf7c470e8b1dd49beead9b4"
    },
    "src/coordharness/jobs/status.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Standalone configurable process patterns, contained paths, repository roots, and done-signal resolution materially replace source-layout behavior. Re-reviewed after making proof custody general: done_signal_custodied requires a tracked proof for every suffix except a measured exemption set, existence is never waived, the duckdb table signal still short-circuits first, and a directory proof is answered by looking for a tracked file beneath it because git's index holds no directories (agent self-review, not independent human review).",
      "reviewed_sha256": "64ae14a5976ae4c8e72e8ffdbd7feb9cabc1f2d61ffe258cf1da207f66ced845"
    },
    "src/coordharness/knowledge/context_federator.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.knowledge.context_federator)",
      "reviewed_sha256": "ae9ea2bef2fcd1d85584bd20f9a80c36953057f342b7c980701c4f2a66bc55a8"
    },
    "src/coordharness/knowledge/kfts.py": {
      "origin": "ported, then extended in this repository",
      "reason": "import closure of the coordination control plane (coordharness.knowledge.kfts)",
      "reviewed_sha256": "7c7c797cc97198a79f5a1e5ed4d69cfe35ac925d70cbbf75da7861b0919c918b"
    },
    "src/coordharness/testing/apfs_cow_cleanroom.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Clean-room defaults use neutral project-local store names and preserve fail-closed APFS clone behavior.",
      "reviewed_sha256": "0cc73b42a53dc26eb64bc12d914c4d9ecb6b09efc70165ff6eee8956c1378de2"
    },
    "src/coordharness/testing/pytest_gate.py": {
      "origin": "ported, then substantially adapted for standalone publication",
      "reason": "Standalone contained and configurable test roots, receipt locations, and sidecar and receipt validation materially extend the pinned gate. Re-reviewed after runner_is_live() started checking process_liveness.POSIX_LIVENESS_PROBE_AVAILABLE before calling os.kill(pid, 0), returning this function's own pre-existing 'cannot verify' sentinel (None) on a platform without a POSIX signal-0 probe instead of calling it and risking a misread of what the null signal means there. The three existing outcomes (True/False/None) and every other branch are unchanged.",
      "reviewed_sha256": "17d0f4b2b472b2de4064e3cc2c6f03eb07b0b3648e9cbde86ebf567267ca99d0"
    },
    "src/coordharness/usage/account_actions.py": {
      "origin": "ported from a pinned private account-action mechanism, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after wiring fixed-loopback standalone account actions with explicit schemas, bounded bodies, and sanitized responses.",
      "reviewed_sha256": "5a6c13add29cb0f7251b0e1851e4d8d55dd4a6bcc9eef63e53353fef5a17d14c"
    },
    "src/coordharness/usage/dashboard_proxy.py": {
      "origin": "adapted from a pinned private provider-usage producer into a standalone read-only proxy boundary",
      "reason": "Re-reviewed after binding the standalone local usage producer, last-good behavior, and neutral endpoint contract; no private ledger or companion-app dependency remains. Re-reviewed again (agent self-review, not independent human review) after daily sanitization gained cache_create_5m_tokens/cache_create_1h_tokens integer fields and an optional model_breakdowns list (bounded, sanitized per-model through the same integer-field and safe-string helpers as the parent row); no relaxation of the existing invalid_contract refusals.\n\nRe-pinned: Read the pace-marker diff for this file: pace sanitisation now passes marker_remaining_percent through the numeric helper bounded at 100 and marker_kind through the safe-token helper against the closed set reserve/on_pace/deficit, raising invalid_contract otherwise. Still read-only, and neither added field escapes the existing bounds. (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)\n\nRe-pinned: Sanitizer admits cost source/observed_at/coverage fields through the same bounded validators; rest is formatting (cross-lane review by claude post-merge) (tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.)",
      "reviewed_sha256": "0db489cd579fddb5b0b5c9e374ee096cda73735595c30fb32cd61a41842ed7b5",
      "repin": {
        "reason": "Sanitizer admits cost source/observed_at/coverage fields through the same bounded validators; rest is formatting (cross-lane review by claude post-merge)",
        "method": "tool-assisted: the hash was updated to the staged bytes by tools/extract/repin.py --apply. This records acceptance of the new content, not an independent re-review of it.",
        "previous_sha256": "adc4a71ca98e9c285862998ad0e1f9a0ad820a2c169b42074c0f3195bfa9d092"
      }
    },
    "src/coordharness/usage/provider_management.py": {
      "origin": "ported from a pinned private provider-management mechanism, then substantially adapted for the standalone harness",
      "reason": "Re-reviewed after routing provider management through generic local transport with strict schemas, bounded payloads, and credential redaction.",
      "reviewed_sha256": "dec7c9a9cea709c06836b07f018631939ca784ff05a8005f8b1587a8dc6c2b70"
    },
    "tests/jobs/test_sidecar_snapshot.py": {
      "origin": "ported, then extended in this repository",
      "reason": "unit tests for the already-ported coordharness.jobs.sidecar_snapshot module (fresher-wins merge, MAX-kept totals, hidden-diagnostic-sidecar isolation). Only import is `from coordharness.jobs import sidecar_snapshot`",
      "reviewed_sha256": "afa87965d979927df297fc8a3f24c62c7288810f80b9902a0b513e3ed1119952"
    }
  }
}
